<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Base Activity theme. Compose draws the real chrome, so we use
        Material3.Light.NoActionBar as a thin shell that doesn't render
        any of its own AppBar or splash decorations.

        windowBackground stays white so there's no flash-of-dark-color
        between launch and first composition.
    -->
    <style name="Theme.TokenCounter" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@android:color/white</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>
</resources>
