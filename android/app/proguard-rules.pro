# Project-specific ProGuard / R8 rules.
# Most modern AndroidX + Compose libraries ship their own consumer rules,
# so this file is intentionally minimal for now. Add app-specific keep
# rules here once we introduce reflection / JSON / DI that needs them.
